// FormWise - single place to configure the backend API base URL.
// Keep host_permissions in manifest.json in sync with this origin.
const FORMWISE_API_BASE_URL = 'https://formwise-demo.preview.emergentagent.com/api';
